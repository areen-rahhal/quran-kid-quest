const Onboarding = () => {
  return (
    <div className="min-h-screen bg-gradient-soft flex items-center justify-center p-4">
      {/* Content cleared - ready for new implementation */}
    </div>
  );
};

export default Onboarding;